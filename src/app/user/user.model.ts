//Creating Type Aliases (the name must start with a capital letter)
//type User = {
  //id: string;
  //avatar: string;
  //name: string
//}
//When dealing with objects, you can create an interface instead of using Type Aliases
export interface User {
    id: string;
    avatar: string;
    name: string
  } 